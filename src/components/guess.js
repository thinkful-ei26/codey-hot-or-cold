import React from 'react';
import GuessForm from './guess-form';

export default function Guess() {
    return (
        <section>
            <h2>Make a guess!</h2>
            <GuessForm />
        </section>
    );
    
}